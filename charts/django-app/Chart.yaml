apiVersion: v2
name: django-chart
description: A Helm chart for Django application on Kubernetes
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: "16.7.18"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled

